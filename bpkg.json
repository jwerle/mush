{
  "name": "mush",
  "version": "0.1.0",
  "description": "Mustache templates for bash",
  "scripts": [ "mush.sh" ],
  "license": "MIT",
  "install": "bpkg run install",
  "commands": {
    "install": "mkdir -p ${PREFIX:-/usr/local}/{bin,share/man/man1} && cp mush.sh ${PREFIX:-/usr/local}/bin/mush && cp mush.1 ${PREFIX:-/usr/local}/share/man/man1",
    "lint": "shellcheck **/*.sh",
    "test": "./test.sh"
  }
}
